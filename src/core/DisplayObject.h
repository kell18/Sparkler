#ifndef DISPLAY_OBJECT_H
#define DISPLAY_OBJECT_H

#include <glm/mat4x4.hpp>

using namespace glm;

namespace raytracer {

    class DisplayObject {
    public:
        // bool                 isIntersectWith(Ray& ray);
        // void                 addTransform(mat4 transform);
        // Shape                getShape();
    };
}

#endif